#include <iostream>
using namespace std;

namespace ariel{
    std::string mat(int num1, int num2, char str1, char str2);
            
    // std::ostream& operator<<(std::ostream& out, const string& s){

    //     return out;
    // }
}